// src/constants/apiBase.ts

// Change this value for different environments (dev, prod, etc.)
const API_BASE = process.env.REACT_APP_API_BASE || "http://localhost:8080/api";

export default API_BASE;
